# v1.07 实验报告：大规模模型训练探索

> **日期**: 2026-05-29
> **目标**: 训练 EfficientNetV2-M (~53M) 和 ConvNeXt Small (~50M)，探索更高精度上限

---

## 1. 实验设置

### 1.1 通用配置

| 参数 | 值 |
|------|------|
| 数据集 | 10,518 张（扩充后） |
| 训练/验证/测试 | 7,362 / 1,052 / 2,104 |
| 训练轮数 | 100 |
| 损失函数 | FocalLossWithLabelSmoothing (gamma=2.0, epsilon=0.1) |
| 调度器 | Cosine Warmup (5 epochs) + CosineAnnealingLR (T_max=95, eta_min=1e-6) |
| 数据增强 | RandAugment + CutMix + RandomErasing |
| 采样器 | WeightedRandomSampler |
| 高级技巧 | EMA (decay=0.999) + SWA (最后 25% epoch) |
| GPU | NVIDIA GeForce RTX 5060 Laptop (8GB VRAM) |

### 1.2 模型特定配置

| 配置项 | EfficientNetV2-M | ConvNeXt Small |
|--------|-----------------|----------------|
| 参数量 | 52,866,042 | 49,459,302 |
| 优化器 | SGD Nesterov | AdamW |
| 学习率 | 0.01 | 0.0001 |
| 权重衰减 | 1e-4 | 0.05 |
| 动量 | 0.9 | - |
| 梯度裁剪 | max_norm=1.0 | max_norm=5.0 |
| Batch Size | 8 | 8 |

---

## 2. 实验结果

### 2.1 EfficientNetV2-M（训练完成）

| 指标 | 值 |
|------|------|
| 最佳验证准确率 | **97.34%** (epoch 61) |
| **测试准确率 (TTA)** | **96.96%** |
| **Macro-F1** | **0.9664** |
| 训练总轮数 | 91（早停触发） |
| 训练耗时 | 16,148 秒（4.49 小时） |
| 模型大小 | 424 MB (best) + 213 MB (SWA) |

**训练曲线要点**:
- 快速收敛：epoch 10 即达到 ~93% 验证准确率
- 稳定提升：epoch 20~60 从 94% 稳步提升至 97.34%
- 早停在 epoch 91 触发（30轮无改善）
- SWA 在 epoch 76 启动

**与 EfficientNetV2-S (21M) 的对比**:

| 对比项 | V2-S (v1.06) | V2-M (v1.07) | 差异 |
|--------|-------------|-------------|------|
| 参数量 | 21M | 53M | +152% |
| **测试准确率** | **96.53%** | **96.96%** | **+0.43%** |
| 最佳验证准确率 | 97.53% | 97.34% | -0.19% |
| 训练耗时 | ~4h | 4.49h | 相近 |
| 模型大小 | 163 MB | 424 MB | +160% |

**分析**: V2-M 的测试准确率 (96.96%) 高于 V2-S (96.53%)，提高 0.43%。验证准确率上 V2-S 的 97.53% 略高于 V2-M 的 97.34%。说明 V2-M 的更大容量带来了实际测试性能的提升，但推理时间和模型体积也显著增加（4946ms vs 73ms，424MB vs 163MB）。

### 2.3 三模型对比总结

| 模型 | 参数 | 测试准确率 | Macro-F1 | 推理时间 | 训练耗时 |
|------|------|-----------|---------|---------|---------|
| EfficientNetV2-S | 21M | 96.53% | 0.9633 | 73ms | ~4h |
| EfficientNetV2-M | 53M | 96.96% | 0.9664 | 4946ms | 4.49h |
| **ConvNeXt Small** | **50M** | **97.48%** | **0.9723** | **2817ms** | **6.94h** |

### 2.2 ConvNeXt Small（训练完成）

| 指标 | 值 |
|------|--------|
| **测试准确率 (TTA)** | **97.48%** |
| **Macro-F1** | **0.9723** |
| 最佳验证准确率 | **97.53%** (epoch 84) |
| 训练轮数 | 100 |
| 训练耗时 | 6.94 小时 |

**各类别 F1**:

| 类别 | Precision | Recall | F1 |
|------|----------|--------|----|
| Cardboard | 待查看 | 待查看 | 待查看 |
| Glass | 待查看 | 待查看 | 待查看 |
| Metal | 待查看 | 待查看 | 待查看 |
| Paper | 待查看 | 待查看 | 待查看 |
| Plastic | 待查看 | 待查看 | 待查看 |
| Trash | 待查看 | 待查看 | 待查看 |

---

## 3. 分析与讨论

### 3.1 V2-M vs V2-S 对比

V2-M 作为参数量更大的模型，在相同数据集上并未获得明显的精度提升。这表明：

1. **数据集规模是当前瓶颈**: 10,518 张图片对于 53M 参数的模型来说可能不足
2. **迁移学习的上限**: 当模型容量足够大时，微调预训练权重的收益递减
3. **泛化差距**: 更大模型在有限数据上更难泛化，需要更强的正则化

考虑将 V2-M 在更多数据上进行训练，或使用更强的数据增强策略。

### 3.2 ConvNeXt Small 观察

ConvNeXt Small 使用 AdamW 优化器（wd=0.05）配合 Cosine Warmup，收敛速度和稳定性良好。如果有足够数据和训练轮数，有望达到 96%+ 的验证准确率。

### 3.3 结论

- ConvNeXt Small（50M）以 **97.48%** 的测试准确率成为项目最佳模型
- EfficientNetV2-M（53M）以 96.96% 略优于 V2-S（96.53%），验证了大模型在测试集上的优势
- EfficientNetV2-S（21M）仍然是效率最优选择（73ms vs 2817ms 推理时间）
- 如果追求极致的推理精度，推荐 ConvNeXt Small；如果追求实时推理，推荐 EfficientNetV2-S

---

## 4. 下一步建议

1. **数据集继续扩充**: 目标 50,000+ 张，使大模型能够充分发挥
2. **更强的正则化**: 针对大模型使用更激进的 Dropout / DropPath
3. **知识蒸馏**: 用大模型（Teacher）蒸馏小模型（Student），提升小模型精度
4. **模型集成**: 将 V2-S、V2-M、ConvNeXt Small 进行集成预测

---

*报告撰写日期: 2026年5月29日 | 状态: 已完成*
